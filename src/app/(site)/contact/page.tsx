import StubPage from "@/components/StubPage";

export const metadata = { title: "Contact — Madhav KRG Group" };

export default function ContactPage() {
  return (
    <StubPage
      eyebrow="Contact"
      heading="Get in touch with Madhav KRG Group."
      body="Plant location, sales and general enquiries, careers — full contact details to follow."
    />
  );
}
